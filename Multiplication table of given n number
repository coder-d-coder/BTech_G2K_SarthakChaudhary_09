#include <stdio.h>
int main() {
    int i,n,temp=1;
    printf("enter n:");
    scanf("%d",&n);
    for (i=1;i<=10;i++){
        temp=n*i;
        printf("%d*%d=%d",n,i,temp);
        printf("\n");
    }

    return 0;
}
